[pytest]
# Run from the project root:  py -3.11 -m pytest
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: end-to-end tests that wire multiple blocks together
